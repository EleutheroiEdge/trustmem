# Copy to .env and fill your own values.
# Never commit real secrets.

# Optional remote embedding providers (BYO keys)
OPENAI_API_KEY=
NVIDIA_API_KEY=

# Optional runtime overrides
TRUSTMEM_DIR=
WORKSPACE=
MEM_DIR=
